# .NET Conventions

- Use async/await for IO; avoid blocking calls.
- Prefer immutable DTOs (records).
- Keep DI registration in Api/Infrastructure.
- Favor interface-driven design.
- Keep tests close to behavior (Unit tests in Tests/).
