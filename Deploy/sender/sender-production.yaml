apiVersion: apps/v1
kind: Deployment
metadata:
  name: sender-production
spec:
  selector:
    matchLabels:
      app: sender-production
  replicas: 2
  template:
    metadata:
      labels:
        app: sender-production
    spec:
      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
            - matchExpressions:
              - key: node_role
                operator: In
                values:
                - appnodes-prod
      containers:
      - name: sender-production
        image: registry.fsel.edu.vn/fsel/api/fsel_src/sender:..........
        ports:
        - containerPort: 80
        - containerPort: 443 
        resources:
          limits:
            cpu: "1"
            memory: 2Gi
          requests:
            cpu: 30m
            memory: 50Mi
        volumeMounts:
        - mountPath: /app/appsettings.Production.json
          name: sender-appsettings-production
          subPath: appsettings.Production.json
      imagePullSecrets:
        - name: gitlab-fsel-secret
      volumes:
      - configMap:
          defaultMode: 420
          name: sender-appsettings-production
        name: sender-appsettings-production


---
apiVersion: v1
kind: Service
metadata:
  name: sender-production
  labels:
spec:
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: http
  - port: 443
    targetPort: 443
    protocol: TCP
    name: https
  selector:
    app: sender-production


