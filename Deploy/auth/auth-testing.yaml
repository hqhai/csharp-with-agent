apiVersion: apps/v1
kind: Deployment
metadata:
  name: auth-testing
spec:
  selector:
    matchLabels:
      app: auth-testing
  replicas: 1 
  template:
    metadata:
      labels:
        app: auth-testing
    spec:
      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
            - matchExpressions:
              - key: node_role
                operator: In
                values:
                - appnodes-staging
      containers:
      - name: auth-testing
        image: registry.fsel.edu.vn/fsel/api/fsel_src/auth:..........
        ports:
        - containerPort: 80
        - containerPort: 443 
        resources:
          limits:
            cpu: "1"
            memory: 2Gi
          requests:
            cpu: 30m
            memory: 50Mi
        volumeMounts:
        - mountPath: /app/appsettings.Testing.json
          name: auth-appsettings-testing
          subPath: appsettings.Testing.json
      imagePullSecrets:
        - name: gitlab-fsel-secret
      tolerations:
      - effect: NoSchedule
        key: appnodes-staging
        operator: Exists
      volumes:
      - configMap:
          defaultMode: 420
          name: auth-appsettings-testing
        name: auth-appsettings-testing

---
apiVersion: v1
kind: Service
metadata:
  name: auth-testing
  labels:
spec:
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: http
  - port: 443
    targetPort: 443
    protocol: TCP
    name: https
  selector:
    app: auth-testing


