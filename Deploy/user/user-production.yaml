apiVersion: apps/v1
kind: Deployment
metadata:
  name: user-production
spec:
  selector:
    matchLabels:
      app: user-production
  replicas: 2
  template:
    metadata:
      labels:
        app: user-production
    spec:
      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
            - matchExpressions:
              - key: node_role
                operator: In
                values:
                - appnodes-prod
      containers:
      - name: user-production
        image: registry.fsel.edu.vn/fsel/api/fsel_src/user:..........
        ports:
        - containerPort: 80
        - containerPort: 443 
        resources:
          limits:
            cpu: "1"
            memory: 2Gi
          requests:
            cpu: 30m
            memory: 50Mi
        volumeMounts:
        - mountPath: /app/appsettings.Production.json
          name: user-appsettings-production
          subPath: appsettings.Production.json
      imagePullSecrets:
        - name: gitlab-fsel-secret
      tolerations:
      - effect: NoSchedule
        key: appnodes-prod
        operator: Exists
      volumes:
      - configMap:
          defaultMode: 420
          name: user-appsettings-production
        name: user-appsettings-production

---
apiVersion: v1
kind: Service
metadata:
  name: user-production
  labels:
spec:
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: http
  - port: 443
    targetPort: 443
    protocol: TCP
    name: https
  selector:
    app: user-production


