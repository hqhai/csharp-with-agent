apiVersion: apps/v1
kind: Deployment
metadata:
  name: realtime-staging
spec:
  selector:
    matchLabels:
      app: realtime-staging
  replicas: 1
  template:
    metadata:
      labels:
        app: realtime-staging
    spec:
      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
            - matchExpressions:
              - key: node_role
                operator: In
                values:
                - appnodes-staging
      containers:
      - name: realtime-staging
        image: registry.fsel.edu.vn/fsel/api/fsel_src/realtime:..........
        ports:
        - containerPort: 80
        - containerPort: 443 
        resources:
          limits:
            cpu: "1"
            memory: 2Gi
          requests:
            cpu: 30m
            memory: 50Mi
        volumeMounts:
        - mountPath: /app/appsettings.Staging.json
          name: realtime-api-appsettings-staging
          subPath: appsettings.Staging.json
      imagePullSecrets:
        - name: gitlab-fsel-secret
      tolerations:
      - effect: NoSchedule
        key: appnodes-staging
        operator: Exists
      volumes:
      - configMap:
          defaultMode: 420
          name: realtime-api-appsettings-staging
        name: realtime-api-appsettings-staging

---
apiVersion: v1
kind: Service
metadata:
  name: realtime-staging
  labels:
spec:
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: http
  - port: 443
    targetPort: 443
    protocol: TCP
    name: https
  selector:
    app: realtime-staging

---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: realtime-staging-main
  labels:
    app.name: realtime-staging
  annotations:
    nginx.ingress.kubernetes.io/client-body-buffer-size: 1024M
    nginx.ingress.kubernetes.io/cors-allow-headers: DNT,X-CustomHeader,Keep-Alive,User-Agent,X-Requested-With,If-Modified-Since,Cache-Control,Content-Type,Authorization,Timeline,IdToken,X-Token,X-Time,Client-ID,Token,formdata,transaction-name,access-control-allow-origin
    nginx.ingress.kubernetes.io/cors-allow-origin: '*'
    nginx.ingress.kubernetes.io/enable-cors: "true"
    nginx.ingress.kubernetes.io/proxy-body-size: 1024m
    nginx.ingress.kubernetes.io/proxy-connect-timeout: "3600"
    nginx.ingress.kubernetes.io/proxy-read-timeout: "3600"
    nginx.ingress.kubernetes.io/proxy-send-timeout: "3600"
spec:
  ingressClassName: nginx-public
  rules:
  - host: fsel-realtime-beta-api.fsel.edu.vn
    http:
      paths:
      - path: /
        pathType: ImplementationSpecific
        backend:
          service:
            name: realtime-staging
            port:
              number: 80
  tls:
  - hosts:
    - fsel-realtime-beta-api.fsel.edu.vn
    secretName: fsel-edu-vn-tls-wildcard



