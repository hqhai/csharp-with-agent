apiVersion: apps/v1
kind: Deployment
metadata:
  name: lmsapi-staging
spec:
  selector:
    matchLabels:
      app: lmsapi-staging
  replicas: 1
  template:
    metadata:
      labels:
        app: lmsapi-staging
    spec:
      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
            - matchExpressions:
              - key: node_role
                operator: In
                values:
                - appnodes-staging
      containers:
      - name: lmsapi-staging
        image: registry.fsel.edu.vn/fsel/api/fsel_src/lmsapi:..........
        ports:
        - containerPort: 80
        - containerPort: 443 
        resources:
          limits:
            cpu: "1"
            memory: 2Gi
          requests:
            cpu: 30m
            memory: 50Mi
        volumeMounts:
        - mountPath: /app/appsettings.Staging.json
          name: lmsapi-appsettings-staging
          subPath: appsettings.Staging.json
      imagePullSecrets:
        - name: gitlab-fsel-secret
      tolerations:
      - effect: NoSchedule
        key: appnodes-staging
        operator: Exists
      volumes:
      - configMap:
          defaultMode: 420
          name: lmsapi-appsettings-staging
        name: lmsapi-appsettings-staging

---
apiVersion: v1
kind: Service
metadata:
  name: lmsapi-staging
  labels:
spec:
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: http
  - port: 443
    targetPort: 443
    protocol: TCP
    name: https
  selector:
    app: lmsapi-staging


