apiVersion: apps/v1
kind: Deployment
metadata:
  name: notification-dev
spec:
  selector:
    matchLabels:
      app: notification-dev
  replicas: 1 
  template:
    metadata:
      labels:
        app: notification-dev
    spec:
      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
            - matchExpressions:
              - key: node_role
                operator: In
                values:
                - appnodes-staging
      containers:
      - name: notification-dev
        image: registry.fsel.edu.vn/fsel/api/fsel_src/notification:auth-openid-...............
        ports:
        - containerPort: 80
        - containerPort: 443 
        resources:
          limits:
            cpu: "1"
            memory: 2Gi
          requests:
            cpu: 30m
            memory: 50Mi
      tolerations:
      - effect: NoSchedule
        key: appnodes-staging
        operator: Exists
      imagePullSecrets:
        - name: dev-image-secret

---
apiVersion: v1
kind: Service
metadata:
  name: notification-dev
  labels:
spec:
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: http
  - port: 443
    targetPort: 443
    protocol: TCP
    name: https
  selector:
    app: notification-dev