apiVersion: apps/v1
kind: Deployment
metadata:
  name: hangfire-production
spec:
  selector:
    matchLabels:
      app: hangfire-production
  replicas: 2
  template:
    metadata:
      labels:
        app: hangfire-production
    spec:
      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
            - matchExpressions:
              - key: node_role
                operator: In
                values:
                - appnodes-prod
      containers:
      - name: hangfire-production
        image: registry.fsel.edu.vn/fsel/api/fsel_src/hangfire:..........
        ports:
        - containerPort: 80
        - containerPort: 443 
        resources:
          limits:
            cpu: "1"
            memory: 2Gi
          requests:
            cpu: 30m
            memory: 50Mi
        volumeMounts:
        - mountPath: /app/appsettings.Production.json
          name: hangfire-appsettings-production
          subPath: appsettings.Production.json
      imagePullSecrets:
        - name: gitlab-fsel-secret
      volumes:
      - configMap:
          defaultMode: 420
          name: hangfire-appsettings-production
        name: hangfire-appsettings-production

---
apiVersion: v1
kind: Service
metadata:
  name: hangfire-production
  labels:
spec:
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: http
  - port: 443
    targetPort: 443
    protocol: TCP
    name: https
  selector:
    app: hangfire-production


