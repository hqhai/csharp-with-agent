apiVersion: apps/v1
kind: Deployment
metadata:
  name: interaction-dev
spec:
  selector:
    matchLabels:
      app: interaction-dev
  replicas: 1 
  template:
    metadata:
      labels:
        app: interaction-dev
    spec:
      containers:
      - name: interaction-dev
        image: registry.fsel.edu.vn/fsel/api/fsel_src/interaction:auth-openid-...............
        ports:
        - containerPort: 80
        - containerPort: 443 
      imagePullSecrets:
        - name: dev-image-secret

---
apiVersion: v1
kind: Service
metadata:
  name: interaction-dev
  labels:
spec:
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: http
  - port: 443
    targetPort: 443
    protocol: TCP
    name: https
  selector:
    app: interaction-dev

---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: interaction-dev
  annotations:
    nginx.ingress.kubernetes.io/backend-protocol: "HTTPS"
    nginx.ingress.kubernetes.io/force-ssl-redirect: "true"
    nginx.ingress.kubernetes.io/ssl-passthrough: "true"
    nginx.ingress.kubernetes.io/x-forwarded-proto: "https"
    nginx.ingress.kubernetes.io/enable-cors: "true"
    nginx.ingress.kubernetes.io/cors-allow-methods: "PUT, GET, POST, OPTIONS"
    nginx.ingress.kubernetes.io/cors-allow-origin: "*"
    nginx.ingress.kubernetes.io/cors-allow-headers: "DNT,X-CustomHeader,Keep-Alive,User-Agent,X-Requested-With,If-Modified-Since,Cache-Control,Content-Type,interactionorization,Timeline,IdToken,X-Token,X-Time,Client-ID,Token,formdata"
spec:
  ingressClassName: nginx-public
  rules:
  - host: fsel-interaction-dev.fsel.edu.vn
    http:
      paths:
      - path: /
        pathType: ImplementationSpecific
        backend:
          service:
            name: interaction-dev
            port:
              number: 443
  tls:
  - hosts:
    - fsel-interaction-dev.fsel.edu.vn
    secretName: fsel-interaction-dev.fsel.edu.vn-wildcard

status:
  loadBalancer:
    ingress:
    - hostname: 103.22.218.217.nip.io



