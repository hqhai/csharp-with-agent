apiVersion: apps/v1
kind: Deployment
metadata:
  name: system-api
spec:
  replicas: 1
  selector:
    matchLabels:
      app: system-api
  template:
    metadata:
      labels:
        app: system-api
    spec:
      containers:
      - name: system
        image: fselacr.azurecr.io/systemapi
        imagePullPolicy: Always
        ports:
        - containerPort: 80
# service type loadbalancer       
---
apiVersion: v1
kind: Service
metadata:
  name: system-api-svc
spec:
  selector:
    app: system-api
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: LoadBalancer