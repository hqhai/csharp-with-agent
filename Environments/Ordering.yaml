apiVersion: apps/v1
kind: Deployment
metadata:
  name: ordering-api
spec:
  replicas: 2
  selector:
    matchLabels:
      app: ordering-api
  template:
    metadata:
      labels:
        app: ordering-api
    spec:
      containers:
      - name: ordering
        image: fselacr.azurecr.io/orderingapi
        imagePullPolicy: Always
        ports:
        - containerPort: 80
# service type loadbalancer       
---
apiVersion: v1
kind: Service
metadata:
  name: ordering-api-svc
spec:
  selector:
    app: ordering-api
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: LoadBalancer